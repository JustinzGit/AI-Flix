<div class="title">
  <h3>Generate a Movie Playlist</h3>
</div>

<div class="data">
  <p>AI-Flix generates a movie playlist by finding the shortest path of movies that connect two actors.</p>
  <%= form_tag playlists_path do %>
    <p><b><%= label_tag "Actor 1:" %></b>
    <%= collection_select nil, :actor_1, Actor.all.order(:name), :id, :name %></p>

    <p><b><%= label_tag "Actor 2:" %></b>
    <%= collection_select nil, :actor_2, Actor.all.order(:name), :id, :name %></p>

    <p><%= submit_tag "Generate Playlist" %></p>
  <% end %>
</div>
