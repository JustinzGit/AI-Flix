<div class="title">
  <h3>Generate a Movie Path</h3>
</div>

<div >
  <p>AI-Flix generates a movie path by finding the shortest path of movies that connect two actors.</p>
  <p><b>Actor 1: </b><%= actor_1_name%></p>
  <p><b>Actor 2: </b><%= actor_2_name %></p>
  <p><%= link_to "Clear Actors", clear_actor_names_path %></p>

  <% if actor_1_name && actor_2_name %>
    <p><%= button_to "Generate Path", paths_path, method: :POST %></p>

  <% else %>
    <%= form_tag new_user_path_path, method: :get do %>
      <p><%= label_tag :name, "Actor Name" %><br>
      <%= text_field_tag :name, params[:name] %></p>

      <p><%= submit_tag "Search" %></p>
    <% end %>
  </div>

  <div class="list">
    <% if @actors %>
      <h3 class="list-title">We found <%= @actors.count %> Actors</h3>
        <% @actors.each do |a| %>
            <p><%= link_to "#{a.name}", actor_action(a) %> - <%= link_to "Actor Page", actor_path(a) %></p>
        <% end %>
    <% end %>
  </div>
<% end %>
